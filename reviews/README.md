# Reviews

This folder stores external review requests and review results generated by Codex PMO.

Default files are generated by:

```powershell
python scripts/external_review.py --target docs/TOOL_CONTEXT_GUIDE.md --question "Review this change"
```

Review outputs are advisory. Codex summarizes them in the PMO report and decides the next action with the user or the owning tool.
